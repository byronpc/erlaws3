[
  {erlaws3, [
    {access_key, ""},
    {secret_key, ""},

     % number of retries to upload parts before giving up
     {max_retry, 3},

     % split into chunk size for multipart uploads (range 5MB - 5GB)
     {chunk_size, 5242880}
  ]}
].